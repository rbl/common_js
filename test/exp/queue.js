require.paths.unshift(__dirname);
require.paths.unshift(__dirname + '/../..');

var L = require("log");
var PK = require("pk");
var Queue = require("queue");


exports['test sendMessage'] = function(beforeExit)
{
  
}